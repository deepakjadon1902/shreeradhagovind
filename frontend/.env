# Frontend env — copy to .env and fill in.
# Leave VITE_API_URL empty to keep using the localStorage demo mode.
VITE_API_URL=
# Used by /api/auth/google (Google Identity Services). Optional.
VITE_GOOGLE_CLIENT_ID=
